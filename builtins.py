property 
the property() takes four parameters:
fget - function for geting an attribute value
fset - function for setting an attribute value
fdel - function for deleting an attribute value
doc - a string that contains documents

range
the range() function returns a sequence of numbers, starting from 0 by default
and cremetal by 1 by default  and stops before a  specified number.

reversed
the reversed() function returns an iterator that accesses the given sequence in
in the reverse order.
  
